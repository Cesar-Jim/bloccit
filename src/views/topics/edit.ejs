<!DOCTYPE html>
<html>

<head>
   <% include ../static/partials/head.ejs %>
</head>

<body>
   <% include ../static/partials/navbar.ejs %>

   <main class="container">

      <h1>Edit Topic</h1>

      <!-- HTML forms do not send requests using the PUT HTTP verb, so we use POST instead. -->
      <form action="/topics/<%= topic.id %>/update" method="post">
         <div class="form-group">
            <label for="title">Topic title</label>

            <!-- We set the value attribute of the input to the title of the topic we want to edit. -->
            <input value="<%= topic.title %>" type="text" class="form-control" name="title" aria-describedby="titleHelp"
               placeholder="Enter Title">
         </div>
         <div class="form-group">
            <label for="description">Description</label>

            <!-- For textarea tags, we set the value inside the tag. -->
            <textarea rows="4" class="form-control" name="description" placeholder="Enter Description"><%= topic.description %></textarea>
         </div>
         <button type="submit" class="btn btn-primary">Update</button>
      </form>
   </main>

   <% include ../static/partials/baseScripts.ejs %>
</body>

</html>